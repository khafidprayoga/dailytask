const app = require('@app');
const request = require('supertest');
const serverRouter = require('../index');

describe('/register endpoint to handle new user', () => {
  it('/register endpoint should found! with post method', () => {});
});
