const app = require('@app');
const request = require('supertest');
const serverRouter = require('../index');

describe('/login endpoint to handle login session', () => {
  it('/login endpoint should found! with post method', () => {});
});
